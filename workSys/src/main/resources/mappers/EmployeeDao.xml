<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangfuzeng.employee.dao.EmployeeDao">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wangfuzeng.employee.po.EmployeePO">
        <id column="Id" property="id" />
        <result column="emp_name" property="empName" />
        <result column="sex" property="sex" />
        <result column="age" property="age" />
        <result column="dept_name" property="deptName" />
        <result column="emp_degree_name" property="empDegreeName" />
        <result column="possword" property="password" />
        <result column="salt" property="salt" />
    </resultMap>

    <!-- 通用查询结果列  -->
    <sql id="BaseColumnList">
        Id, emp_name, sex, age, dept_name, emp_degree_name, possword, salt
    </sql>
</mapper>
